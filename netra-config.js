// NETRA API Configuration
// Currently DISCONNECTED from n8n backend (running in local / standalone mode).
// To reconnect, uncomment the URLs below and set them in window.NETRA_CONFIG.

window.NETRA_CONFIG = {
  // Retinal image analysis endpoint (multipart/form-data POST)
  // Set to: 'https://adhihackathon.app.n8n.cloud/webhook/diabetic-retinopathy/screen'
  screenUrl: '',

  // Referral queue endpoint (GET)
  // Set to: 'https://adhihackathon.app.n8n.cloud/webhook/diabetic-retinopathy/referral-queue'
  referralQueueUrl: '',
};
